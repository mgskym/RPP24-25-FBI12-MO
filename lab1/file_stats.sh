if [ -f "$1" ]; then
    ll= wc -l "$1"
    lw= wc -w "$1"
    lm= wc -m "$1"
    echo "Количество строк: $lc"
    echo "Количество слов: $lw"
    echo "Количество символов: $lm"
else
    echo "Ошибка. Файл не существует или не указан"
fi
